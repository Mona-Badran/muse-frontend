import 'package:flutter/material.dart';

class ArchImage extends StatelessWidget {
  const ArchImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
